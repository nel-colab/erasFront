services:
  frontend:
    build: .
    image: eras-frontend:latest
    container_name: eras_frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - eras-network

networks:
  eras-network:
    external: true
